Project Path: Joblooper

Source Tree:

```
JobLooper
├── app
│   ├── api
│   ├── components
│   │   ├── exampleHeader.js
│   │   └── ui
│   │       └── exampleButton.js
│   ├── favicon.ico
│   ├── fonts
│   │   ├── GeistMonoVF.woff
│   │   └── GeistVF.woff
│   ├── globals.css
│   ├── layout.js
│   └── page.js
├── generate_codebase_complete.bat
├── jsconfig.json
├── lib
│   ├── db
│   │   └── prisma.js
│   └── hooks
│       └── useDebounce.js
├── next.config.mjs
├── output.md
├── package-lock.json
├── package.json
├── prisma
│   └── schema.prisma
├── public
├── README.md
└── styles
    ├── globals.css
    └── reset.css

```

`\\?\C:\Dev\Jobloop\joblooper2\JobLooper\app\components\exampleHeader.js`:

```js
/**
 * REACT COMPONENTS BRUKER PASCAL CASE FOR KOMPONENT NAVN. PASCAL CASE EXAMPLE ( PascalCaseComponent )
 *
 * Stor forbokstav på komponent, alle ord etter har også storbokstav.
 * Denne komponenten ligger rett inne i components mappen fordi det er en stor komponent eller en komponent
 * som ikke er veldig gjenbrukbar ( har som regel bare et bruksområde, som denne headeren. )
 */

export default function Header() {
	return (
		<header>
			<h1>
				Header component example. Lives in the components folder since its not
				very reusable.
			</h1>
		</header>
	);
}

```

`\\?\C:\Dev\Jobloop\joblooper2\JobLooper\app\components\ui\exampleButton.js`:

```js
/**
 * REACT UI COMPONENT, BRUKER PASCAL CASE FOR KOMPONENT NAVN. PASCAL CASE EXAMPLE ( PascalCaseComponent )
 *
 * Ui komponenter er komponenter som kan brukes på mange forksjellige plasser i nettsiden
 * en button er et eksempel på en ui komponent.
 */

export default function Button() {
	return <button>Min gjenbrukbare button komponent.</button>;
}

```

`\\?\C:\Dev\Jobloop\joblooper2\JobLooper\app\layout.js`:

```js
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}

```

`\\?\C:\Dev\Jobloop\joblooper2\JobLooper\app\page.js`:

```js
export default function Home() {
	return <></>;
}

```

`\\?\C:\Dev\Jobloop\joblooper2\JobLooper\lib\db\prisma.js`:

```js
import { PrismaClient } from "@prisma/client";

const globalForPrisma = global;

export const prisma = globalForPrisma.prisma ?? new PrismaClient(); // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing

if (process.env.NODE_ENV !== "production") {
	globalForPrisma.prisma = prisma;
}

```

`\\?\C:\Dev\Jobloop\joblooper2\JobLooper\lib\hooks\useDebounce.js`:

```js
"use client";

import { useEffect, useState } from "react";

export const useDebounce = (value, delay = 500) => {
	const [debouncedValue, setDebouncedValue] = useState(value);

	useEffect(() => {
		const timeout = setTimeout(() => {
			setDebouncedValue(value);
		}, delay);

		return () => clearTimeout(timeout);
	}, [value, delay]);

	return debouncedValue;
};

/**
 * Debounce hook
 *
 * Tar input og delayer signalet slik at man kan velge selv hvilken oppdaterings syklus man vil ha
 * Default vaule er 500ms,
 */

```

`\\?\C:\Dev\Jobloop\joblooper2\JobLooper\prisma\schema.prisma`:

```prisma
// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// DATABASE SCHEMA
// VarChar(nummer) er en type text, 40 betyr at dette feltet kan bare inneholde 40 tegn.
// Burde sjekke data som kommer inn i databasen før man kjører npm prisma db push.
// Hvis man er usikker eller ikke vet hvor mye text som kommer inn fra dataen
// Kan man heller bytte @db.VarChar(nummer) til @db.Text.
// @db.Text skal ha plass til uendelig tekst mengde men bruker mer plass i databasen.
// Har brukt @db.VarChar() for enkelte felt. Det er bare en placeholder for øyeblikket siden jeg ikke har sjekket data fra apien.

// Database URL må oppdateres i .env filen til riktig database URL.
// Når Database URL er oppdatert kan man kjøre npm prisma db push, dette generer også en prisma client automatisk.

model BrregApiData {
  id             Int              @id @default(autoincrement())
  kommune        String           @db.VarChar(40)
  adresse        String           @db.VarChar(40)
  telefonNummer  String           @unique @db.VarChar(20)
  epost          String           @unique @db.VarChar(100)
  nettside       String           @unique @db.VarChar(255)
  oppstart       String           @db.VarChar(15)
  industri       String           @db.VarChar(40)
  selskapInfo    String           @db.Text
  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt
  webCrawlerData WebCrawlerData[]
}

model WebCrawlerData {
  id               Int          @id @default(autoincrement())
  jobTittel        String       @db.VarChar(40)
  ledigeStillinger String       @db.VarChar(40)
  skills           String       @db.VarChar(255)
  jobBeskrivelse   String       @db.Text
  deadline         String       @db.VarChar(40)
  urlStilling      String       @unique
  createdAt        DateTime     @default(now())
  updatedAt        DateTime     @updatedAt
  brregApiData     BrregApiData @relation(fields: [brregApiDataId], references: [id])
  brregApiDataId   Int          @unique
}

```